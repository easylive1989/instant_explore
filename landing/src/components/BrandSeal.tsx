/// The Lorescape app icon, rendered as a rounded tile by the
/// `.brand .seal` / `.foot__brand .seal` containers.
export default function BrandSeal() {
  return <img src="/images/app_icon.png" alt="" />;
}
